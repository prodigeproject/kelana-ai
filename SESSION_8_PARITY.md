# Session 8 parity record

Implemented from the PPT flow: users table model, bcrypt password hashing, register, login, JWT token creation, current-user lookup, protected trip reads, backend-owned user_id, and CRUD ownership checks.

Homework: update and delete reject trips owned by another user with HTTP 403.
